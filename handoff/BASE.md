# Base Context

This repo implements the Almost Living Context reference service from `BRIEF.md`.

Durable facts should live in the substrate once the service is running. This file is only a seed handoff base
for repository work before the service has its own data.
